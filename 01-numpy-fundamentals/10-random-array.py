import numpy as np


#making an array of random values


array=np.random.rand(2) # 2 elemts in one row
print(array)

array2=np.random.rand(3,3)  # 3 by 3 matrix of random numbers , notice that the random numbers are floats so as if we wanna add any value to them later, there's no issue
print(array2)